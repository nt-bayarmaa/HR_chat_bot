{
	"buildCommand": "bun install && bun run build:rpc",
	"functions": {
		"api/**/*.ts": {
			"runtime": "bun@1.2.20"
		}
	},
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/api/index"
		}
	]
}

